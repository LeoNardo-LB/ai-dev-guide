# 2026-08-24 公开仓库脱敏批次
> 状态：已完结
> 关联：scripts/scan-secrets.sh · scripts/scan-secrets.allow · CHANGELOG [2.3.0]

## 目标

公开仓库前的敏感信息扫描落地：扫描器脚本化、进 selftest 与 CI、扫描干净后建仓推送。

## 过程与证据

1. scan-secrets.sh 五类检测（私钥/token/密钥赋值/内网地址/个人信息）+ allow 白名单；allow 函数路径归一修复（./ 前缀匹配失败 → 剥前缀）
2. 全仓扫描结果：干净（唯一命中是扫描器自身正则，已白名单）——本系统为模板仓无真实凭据，符合预期
3. selftest 增第 2.5 段（敏感扫描，公开仓库强制）；CI 工作流名同步更新
4. 发现 .github/workflows/check.yml 此前写入静默丢失（未在磁盘与 git 中）——重写并验证存在
5. manifest 2.2.0 → 2.3.0

## 完结迁移区

（系统自身基建，无 backlog 卡片）

## 蒸馏

- 脱敏检查必须脚本化并挂进推送链路：人工「我检查过了」在公开仓库场景不可信
- 教训：write 工具调用失败可能静默——关键产物（CI 配置）落地后必须显式验证存在